<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.music_backend.music.mapper.Music_Mapper">

    <select id="Get_URL" parameterType="String" resultType="java.lang.String">
        SELECT url FROM music WHERE name = #{name}
    </select>

</mapper>
